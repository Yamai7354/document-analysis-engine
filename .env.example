# Copy this file to .env and fill in the key for whichever provider you set
# in config.yaml (llm.provider). Only one is required.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=

# Used for downloading the local HuggingFace embedding model, if needed.
HUGGINGFACEHUB_API_TOKEN=
